<!DOCTYPE html>
<html lang="en">

<%- include('../partials/header_narrow') %>

<body>
    <table style="width: 40%; text-align: left; margin-left: auto; margin-right: auto;" cellpadding="2" cellspacing="2">
        <tbody>
            <tr>
                <td>
                    <h1>
                        New Show
                    </h1>
                </td>
            </tr>
            <tr>
                <td style="width: 20%; text-align: right;">
                    <label for="title">Title:</label>
                </td>
                <td>
                    <input type="text" id="title" name="title" value="<%= (typeof show === 'undefined') ? '' : show.title %>">
                    <button onclick="location.href='/'" class="save">Save</button>
                </td>
            </tr>
            <tr>
                <td style="width: 20%; text-align: right;">
                    <label for="description">Description:</label>
                </td>
                <td>
                    <input type="text" id="description" name="description" value="<%= (typeof show === 'undefined') ? '' : show.description %>">
                    <button onclick="location.href='/'" class="cancel">Cancel</button>
                </td>
            </tr>
        </tbody>
    </table>

</body>

</html>